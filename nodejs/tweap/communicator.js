if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}

define(function() {

    var Client = require('./client.js');

    function Communicator(socket, clientManager) {

    }

    return Communicator;
});